# 2026-09-13 工作日志

## 题库剔除珈乐相关条目（docs/quiz-bank.md）

- 按 ben 决策：**整题删除**（不做干扰项替换），共删 14 行。删除后全文件不再出现「珈乐 / 皇珈骑士 / Carol」。
- 删除清单：
  - 珈乐为主角 3 题：珈乐的粉丝名、珈乐的英文名、绿色应援色（答案即珈乐）。
  - 珈乐仅作干扰项 6 题：武道担当（贝拉）、贝拉的粉丝名、向晚的粉丝名、紫色应援色（乃琳）、粉色应援色（嘉然）、乃琳粤语口头禅、贝拉口头禅。
  - Carol 干扰项连带 5 题：英文名题组全部（向晚/嘉然/乃琳/贝拉/珈乐）。
- 题量变化：1000 → 986（A-SOUL 100→86，常识 800，AI 100）。
- 遗留待定：
  1. 「A-SOUL 一共有几名成员 → 5 名」保留未动，隐含珈乐仍在成员序列。
  2. A-SOUL 子题库抽题比例 1:8:1 因题量下降而略偏，后续补题需补回约 14 题。
  3. 尚未跑 `npm run quiz:sync`（ben 明确说游戏题库后续再更新）。
  4. 游戏侧既有珈乐内容仍未处理：`background.md:170` 彩蛋 Boss「珈乐(黑化)」、`README.md:238` 提及。

## 新增 3 道生日题（docs/quiz-bank.md）

- 按 ben 要求新增嘉然 / 贝拉 / 乃琳各一道生日题，插在「A-SOUL 的队长是谁」之后、粉丝名题之前。
- 生日事实（多源一致：维基/Wikiwand、快懂百科、360 百科）：嘉然 3 月 7 日（双鱼座）、贝拉 7 月 17 日（巨蟹座）、乃琳 8 月 8 日（狮子座）；向晚 6 月 12 日（双子座），未出题。
- 干扰项设计：每题用「其他成员真实生日 + 一个同月/同日的近似日期」，三题不共用同一选项组。
- 题量：986 → 989（A-SOUL 86 → 89，常识 800，AI 100）。
- 校验：用与 `scripts/sync-quiz.mjs` 同规则的临时脚本跑了格式校验，0 错误、无重复题干、无同题重复选项。
- 仍未运行 `npm run quiz:sync`，`src/data/quiz.generated.json` 保持旧版。

## 新增 3 道 2026 生日会题（docs/quiz-bank.md）

- 名称已按官方口径核实（A-SOUL_Official 微博日程表原文 + 录播标题 + ASwiki 交叉验证）：
  - 嘉然生日会 2026-03-14「以爱之名」（14000 舰 / 营收 270.1 万）
  - 贝拉生日会 2026-07-25「八音盒少女」
  - 乃琳生日会 2026-08-29「幻梦叠影」（鸣潮赞助）
- 干扰项设计：每题放本人**线下专场**名称当诱饵 —— 乃琳 3-28「幻变梦境」、贝拉 4-18「在月光下加冕」、嘉然 6-20「然有引力」。注意「幻变梦境」vs「幻梦叠影」极易混。
- 生日会日期 ≠ 设定生日（乃琳 8-8 生日 / 8-29 生日会；贝拉 7-17 / 7-25；嘉然 3-7 / 3-14），出题时别混用。
- 题量：989 → 992（A-SOUL 89 → 92）。校验 0 错误。
- 复用要点：校验用临时脚本复刻 `scripts/sync-quiz.mjs` 的解析规则（6 列、答案 A-D、非空、选项不重复、题干不重复），**不写** `quiz.generated.json`。

## 玩家意见反馈系统（主界面 → 阿里云服务器落库）

按 ben 要求做的整套反馈系统，改动清单：

- 服务端 `server/game_gate.py`：新增 `POST /api/feedback`，追加写 `/opt/game-gate/feedback.jsonl`（可用 `GAME_FEEDBACK_LOG` 覆盖）。
  - 校验：去控制字符保留换行、截断 `GAME_FEEDBACK_MAX_CHARS`(500)、空内容 400。
  - 限频：同 `cid`（无 cid 退回 IP）`GAME_FEEDBACK_COOLDOWN`(10s) 内只允许一次，超了 429 `too-fast`。
  - 去重：启动时读入最后 500 条 id，客户端补交同 id 直接回 `duplicate`。
  - GET 只回 `{"ok":true,"count":n}`，不回放内容（`/game/api/` 公网可达，不能泄露别人的反馈）。
  - 记录字段：id/ts/time/content/contact/cid/ip（取 X-Forwarded-For 首段）/ua/online。
  - 注意：`do_POST` 原先无条件丢弃请求体，现在改成存入 `self._body` 再交给路由解析。
- 客户端 `src/core/Feedback.ts`：POST 到 `api/feedback`（dev 走 vite proxy → 8793，线上走 nginx `/game/api/`）。
  - 失败分两类：4xx（内容问题）不重试；网络/5xx 存入 localStorage `asoul-feedback-pending-v1`，下次进反馈页自动补交。
  - 复用了 `AdmissionGate` 的 `clientId()`（已从私有函数改为 export）。
- 输入框 `src/ui/DomField.ts`：在画布上浮一个原生 `<textarea>`/`<input>`。
  - **关键决策**：Phaser 的键盘事件收不到中文输入法，自由文本必须走真实 DOM 控件。
  - 坐标换算用 `canvas.getBoundingClientRect()` 反推缩放比，ResizeObserver 观察画布复位。
  - 没有启用 Phaser 的 `dom.createContainer`，避免动到全局配置影响其它场景。
- 页面 `src/scenes/FeedbackScene.ts` + 主界面入口。
  - **入口位置踩过的坑**：原想把「意见反馈」加进底部 4 卡图标栏变成 5 卡，但居中后最左卡会压住 x=314 的角色立绘约 74px，间距压到 90px 以下又太挤 → 最终改成与「开发者模式」并排的胶囊按钮（开发者模式 x=490、意见反馈 x=790，状态文字仍居中 640）。
- 顺手修的既有缺陷：`src/ui/FreshTheme.ts` 的 `createFreshPanel(accent = FRESH.BLUE)`，因 `FRESH` 是 `as const`，默认值把参数类型推成字面量 `6474472`，传别的主题色直接编译报错。加了显式 `accent: number`。

验证：9 项接口断言全过（含去重/限频/空内容/query 回退/入场券无回归）+ headless 浏览器跑通真实提交链路，落库内容含换行。

## 本地 UI 验证方法（踩坑记录，下次照抄）

Windows 上用系统自带 Edge 做 headless 截图，**不需要装 agent-browser（要下 500MB Chromium）**：

```bash
"/c/Program Files (x86)/Microsoft/Edge/Application/msedge.exe" \
  --headless=new --no-sandbox --disable-gpu --no-first-run --hide-scrollbars \
  --force-device-scale-factor=1 \
  --user-data-dir="D:\game\asoulVsA87\.shots\p1" \
  --screenshot="D:\game\asoulVsA87\.shots\x.png" \
  --window-size=1280,800 --virtual-time-budget=25000 "http://127.0.0.1:5175/"
```

四个必须知道的点：

1. **要 `dangerouslyDisableSandbox`**：沙箱内 Edge 进程能起但写不出文件（静默失败、退出码 0）。
2. **截图是异步落盘的，约 30~70 秒后才出现**，必须轮询等待；早期看着"失败"其实只是还没写完。
3. **vite 默认只监听 `[::1]`**，Edge 访问 `127.0.0.1` 会 ERR_CONNECTION_REFUSED → 用 `npm run dev -- --host 127.0.0.1 --port 5175`。
4. **每个 Edge 实例必须用独立 `--user-data-dir`**，否则第二个实例会交接给第一个然后什么都不写。
5. 入场券限流会把 headless 页面挡在门外（`GAME_MAX_SLOTS=3` 三个标签页就满了）→ 本地测试用 `GAME_MAX_SLOTS=20`。

要截某个非首屏页面（如反馈页）时，临时在 `MenuScene.create()` 末尾加 `this.time.delayedCall(200, () => openChildScene(this, FeedbackScene.KEY));`，截完**必须删掉**（`grep -rn "TEMP-SHOT" src/` 自查）。

